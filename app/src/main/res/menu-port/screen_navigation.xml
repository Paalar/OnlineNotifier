<?xml version="1.0" encoding="utf-8"?>

<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/screen_navigation_office"
        android:title="@string/office_menu"
        app:showAsAction="ifRoom"
        android:enabled="true"
        />

    <item
        android:id="@+id/screen_navigation_cantina"
        android:title="@string/cantina_menu"
        app:showAsAction="ifRoom"
        android:enabled="true"/>

    <item
        android:id="@+id/screen_navigation_meetings"
        android:title="@string/meetings_menu"
        app:showAsAction="ifRoom"
        android:enabled="true"/>
</menu>